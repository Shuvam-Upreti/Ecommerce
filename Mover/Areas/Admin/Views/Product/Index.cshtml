@using Mover.Areas.Admin.ViewModel.Product
@model List<ProductViewModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Index";
}

@* 
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Product List</h2>
        </div>

        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i>&nbsp Create New Product
            </a>

        </div>
        <hr />
    </div>
    <table id="tbldata" class="table table-bordered table-striped table-hover " style="width:100%">
        <thead>
            <tr>
                <th>Name</th>
                <th>Desc</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>

                    <td width="40%">
                        @obj.ProductName
                    </td>
                    <td width="20%">
                        @obj.Description
                    </td>
                    <td style="display:flex;">
                        <div class="w-50 btn-group" role="group">
                            <a asp-area="Admin" asp-controller="Product" asp-action="Edit" asp-route-id="@obj.ProductId" class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i>&nbsp Edit</a>
                        </div>
                        <form method="post" asp-area="Admin" asp-controller="Product" asp-action="Delete" asp-route-productId="@obj.ProductId">
                            <button type="submit" class="btn btn-danger mx-2">
                                <i class="bi bi-trash"></i>&nbsp Delete
                            </button>
                        </form>


                    </td>

                </tr>
            }

        </tbody>
    </table>
</div> *@
<div class="searchBar">
    <div class="loHead">
        <span>Products</span>
        <div class="col-12">
            <a href="/admin/product/create" class="btn btn-primary pull-right">
                <i class="fa fa-plus"></i> Create New Product
            </a>
        </div>
    </div>
</div>
<div id="productGrid" class="panel" style="min-height:500px"></div>


@section scripts {

    <script src="~/js/Product/Product.js" asp-append-version="true"></script>

}
